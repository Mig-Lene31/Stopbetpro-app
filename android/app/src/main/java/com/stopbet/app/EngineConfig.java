package com.stopbet.app;

public class EngineConfig {

    // TRUE = modo teste (não bloqueia de verdade)
    // FALSE = modo final (bloqueio real)
    public static final boolean ENGINE_TEST_MODE = true;

    // Margem de tolerância para leitura de valores
    public static final float VALUE_MARGIN = 5.0f;
}
