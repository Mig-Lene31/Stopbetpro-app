package com.stopbet.app;

import android.content.Context;

public class LicenseState {

    // por enquanto sempre válida
    // depois você conecta com pagamento / tempo / servidor
    public static boolean isValid(Context ctx) {
        return true;
    }
}
